import { Box } from '@chakra-ui/react';
import { MarketplaceGridView } from './MarketplaceGrid';

const MarketplaceResults = () => {
  return (
    <Box w="full">
      <MarketplaceGridView />
    </Box>
  );
};

export default MarketplaceResults;